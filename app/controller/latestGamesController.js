(function latestGamesController(angular, undefined) {
    'use strict';

    var app = angular.module('jtfl');

    app.controller('latestGamesController', function($scope, appState) {
    });

}(window.angular));