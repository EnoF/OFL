(function leaderboardController(angular, undefined) {
    'use strict';

    var app = angular.module('jtfl');

    app.controller('leaderboardController', function($scope, appState) {
    });

}(window.angular));